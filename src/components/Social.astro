
---
const { platform, username } = Astro.props;
---
<a class="anchor p-6" href={`https://www.${platform}.com/${username}`}>{platform}</a>

<style>
    a {
      color: var(--primary-color);
      text-decoration: none;
      font-size: 16px;
      font-weight: bold;
      background-color: var(--accent-color);
      border: none;
      border-radius: 7px;
      cursor: pointer;
      transition: background-color 0.3s ease;
    }

  </style>



